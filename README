#Install the following packages:

sudo apt-get install cmake build-essential

#Generate makefile by cmake

mkdir build
cd build
cmake ..

# Compile:

make

# execute:

./Solucao_Elo_Maluco


it's all!



